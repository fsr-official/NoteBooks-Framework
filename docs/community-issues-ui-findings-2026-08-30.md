# Community and Issues UI verification

The local built app was captured at 1440x1000 and 390x844 after the portal renderer and stylesheet update.

Community now presents a large editorial hero, a two-column workspace with a room guide, an introductory context panel, channel navigation, a message room, and a member directory/profile area. Issues now presents a large hero, an issue desk context panel, a proposal guide, a structured proposal form with repository/path and optional line-evidence fields, and the live issue feed with vote controls.

The desktop captures rendered successfully from the actual server port 4000. The initial port-3000 capture was invalid because this project starts on port 4000; it was discarded and recaptured correctly. The visual captures show no runtime error page in the corrected set. Mobile captures were also generated for responsive regression coverage.
